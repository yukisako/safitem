<div class="col-sm-12 page-head">
  <h2><span class="label label-lg thema-color">チャットリスト</span></h2>
</div>

<div>
<% if @shelter_ids.empty? %>
	<p style="color:#aaa;padding-left:30px;">
	支援表明するとその避難所とチャットすることができるようになります</p>
<% else %>
	<ul class="list-group">
	    <% @shelter_ids.each do |shelter_id| %>
	        <% shelter = Shelter.find(shelter_id) %> 
	        <li class="list-group-item">
	            <a href="/chat_room/<%= shelter.id %>"><%= shelter.shelter_name %></a>
	        </li>
	    <% end %>
	</ul>
</div>
<% end %>
<!-- 余裕があれば通知機能とかも作りたい感ある -->